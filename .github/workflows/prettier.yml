# Generated by cue/workflows_tool.cue; do not edit
name: Check for formatting errors in Go HTTP templates
"on":
  push:
    branches:
      - main
    paths:
      - '**.tmpl'
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  lint:
    name: Check for formatting errors in Go HTTP templates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b
      - name: Install prettier
        run: npm install --global prettier prettier-plugin-go-template
      - name: Run prettier
        run: prettier --write ui/html/*.tmpl ui/html/**/*.tmpl
      - name: Check commit is clean
        run: test -z "$(git status --porcelain)" || (git status; git diff; false)
